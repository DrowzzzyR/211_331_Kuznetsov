# Приложение для хранения и контроля целостности товарных накладных

## 📋 Описание проекта

Данное приложение разработано для хранения, контроля целостности и защиты массива записей товарных накладных. Приложение обеспечивает безопасное хранение данных с использованием шифрования AES-256, проверку целостности записей через цепочку MD5 хешей и удобный графический интерфейс для работы с данными.

### Основные возможности

- **Хранение записей товарных накладных** в формате JSON с полями: артикул товара (10 цифр), количество единиц товара, дата и время отгрузки (unix timestamp), хеш MD5 в base64
- **Проверка целостности данных** через цепочку хешей по формуле: `hash_i = MD5(article + quantity + timestamp + hash_i-1)`
- **Визуальное выделение невалидных записей** красным цветом при обнаружении нарушений целостности
- **Шифрование файлов** с использованием алгоритма AES-256-CBC для защиты конфиденциальных данных
- **Автоматическая расшифровка** зашифрованных файлов при загрузке в приложение
- **Адаптивный интерфейс** с использованием QGridLayout для табличного отображения данных

### Технологии

- **Язык программирования:** C++17
- **Фреймворк:** Qt (Widgets)
- **Система сборки:** CMake
- **Шифрование:** OpenSSL (AES-256-CBC)
- **Среда разработки:** Visual Studio 2022, Qt Creator

---

## 🏗️ Архитектура проекта

### Структура файлов

```
211_331_Kuznetsov/
├── CMakeLists.txt              # Конфигурация сборки проекта
├── main.cpp                    # Точка входа в приложение
├── mainwindow.h                # Заголовочный файл главного окна
├── mainwindow.cpp              # Реализация главного окна
├── encryptionmanager.h          # Заголовочный файл менеджера шифрования
├── encryptionmanager.cpp        # Реализация менеджера шифрования
├── config/
│   └── encryption.key          # Ключ шифрования (32 байта)
└── data/
    ├── invoices_valid.json      # Валидные записи (6 записей)
    ├── invoices_invalid.json    # Записи с нарушением целостности
    └── invoices_valid.enc       # Зашифрованный тестовый файл
```

---

## 🔐 Блок 1: Интерфейс и проверка целостности

### Реализованный функционал

1. **Графический интерфейс с QGridLayout**
   - Табличное отображение записей товарных накладных
   - Адаптивная раскладка с автоматическим изменением размеров колонок
   - Заголовки колонок: "Артикул", "Количество", "Дата отгрузки", "Хеш"

2. **Загрузка данных из JSON файла**
   - Автоматическая загрузка при старте приложения
   - Парсинг JSON с валидацией полей
   - Поддержка любого количества записей

3. **Проверка целостности через цепочку хешей**
   - Вычисление MD5 хеша для каждой записи по формуле
   - Проверка соответствия хешей из файла вычисленным значениям
   - Визуальное выделение невалидных записей красным цветом

4. **Кнопка "Открыть" для выбора файла**
   - Диалог выбора файла с поддержкой JSON файлов
   - Загрузка, проверка и отображение данных из выбранного файла

---

## 🔒 Блок 2: Шифрование данных

### Реализованный функционал

1. **Класс EncryptionManager для AES-256-CBC шифрования**
   - Загрузка ключа шифрования из файла (поддержка base64, hex, raw форматов)
   - Шифрование и расшифровка данных с использованием OpenSSL EVP API
   - Генерация случайного IV для каждого шифрования

2. **Интеграция расшифровки в процесс загрузки файлов**
   - Автоматическое определение зашифрованных файлов по расширению `.enc`
   - Расшифровка перед парсингом JSON данных
   - Обработка ошибок расшифровки с выводом сообщений пользователю

3. **Поддержка обычных и зашифрованных файлов**
   - Обычные JSON файлы загружаются без изменений
   - Зашифрованные файлы (`.enc`) автоматически расшифровываются
   - Единый интерфейс для работы с обоими типами файлов

### Формат зашифрованных данных

Зашифрованный файл содержит:
- **IV (Initialization Vector)** — 16 байт случайных данных в начале файла
- **Зашифрованные данные** — данные, зашифрованные алгоритмом AES-256-CBC

При расшифровке IV извлекается из начала файла и используется для расшифровки остальных данных.

---

## 📸 Скриншоты интерфейса

### Скриншот 1: Основное окно приложения с валидными данными

![Основное окно приложения](screenshots/main_window.png)

*Приложение отображает таблицу с записями товарных накладных. Все записи валидны, так как хеши соответствуют вычисленным значениям.*

### Скриншот 2: Отображение невалидных записей

![Невалидные записи](screenshots/invalid_records.png)

*При загрузке файла с нарушенной целостностью невалидные записи и все последующие выделяются красным цветом с белым текстом для визуального выделения.*

---

## 🚀 Сборка и запуск

### Требования

- Qt 5 или Qt 6 (Widgets)
- OpenSSL библиотека
- CMake 3.16 или выше
- Компилятор с поддержкой C++17 (MSVC 2019+, GCC 7+, Clang 5+)

### Сборка проекта

1. Откройте проект в Qt Creator или используйте CMake напрямую
2. Настройте пути к Qt и OpenSSL в CMakeLists.txt (если необходимо)
3. Соберите проект (Build)
4. Запустите исполняемый файл

### Использование

1. При запуске приложение автоматически загружает файл `data/invoices_valid.json`
2. Для загрузки другого файла нажмите кнопку "Открыть"
3. Выберите JSON файл или зашифрованный файл (`.enc`)
4. Приложение автоматически определит тип файла и обработает его соответствующим образом

---

## 📝 Формат данных

### Структура записи товарной накладной

```json
{
  "article": "1234567890",      // Артикул товара (10 цифр)
  "quantity": 10,                // Количество единиц товара
  "timestamp": 1700000000,       // Дата и время отгрузки (unix timestamp)
  "hash": "5rPWVjJBalhBIouaR2Iu3g=="  // Хеш MD5 в base64
}
```

### Формула проверки целостности

```
hash_i = MD5(article + quantity + timestamp + hash_i-1)
```

где:
- `hash_i` — хеш текущей записи
- `hash_i-1` — хеш предыдущей записи (для первой записи — пустая строка)
- Все поля преобразуются в строку и конкатенируются перед вычислением MD5

---

## 🔧 Технические детали

### Шифрование

- **Алгоритм:** AES-256-CBC
- **Размер ключа:** 32 байта (256 бит)
- **Размер IV:** 16 байт
- **Библиотека:** OpenSSL EVP API

### Проверка целостности

- **Алгоритм хеширования:** MD5
- **Кодирование хеша:** Base64
- **Цепочка хешей:** Каждая запись зависит от предыдущей

---

## 📚 Дополнительная информация

Проект разработан в соответствии с требованиями экзаменационной работы, вариант 4. Все компоненты реализованы с использованием современных подходов к разработке на Qt и C++, обеспечивая надёжность, безопасность и удобство использования приложения.

